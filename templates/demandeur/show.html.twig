{% extends 'inside.html.twig' %}

{% block title %}Demandeur{% endblock %}

{% block main %}
    <h1>Demandeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demandeur.id }}</td>
            </tr>
            <tr>
                <th>Entite_demandeur</th>
                <td>{{ demandeur.EntiteDemandeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_demandeur_index') }}">back to list</a>

    <a href="{{ path('app_demandeur_edit', {'id': demandeur.id}) }}">edit</a>

    {{ include('demandeur/_delete_form.html.twig') }}
{% endblock %}
